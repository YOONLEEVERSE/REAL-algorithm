const fs = require("fs");
const [N, M] = fs.readFileSync(0).toString().trim().split(" ").map(Number);

// function getCombination(items) {
//   let result = [];

// for(let j = items)

//   return result;
// }

// let tmp = Array.from({ legnth: N }, (_, idx) => idx + 1);
// for (let i = 2; i <= M; i++) {
//   tmp = getCombination(tmp);
// }

// // 어우 이것도 부

// 모르겟당. ㅎㅎ 다시풀어봐야징 ~! 